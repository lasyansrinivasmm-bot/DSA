class Solution {
    public int[] twoSum(int[] numbers, int target) {
        int start = 0, end = numbers.length - 1, toSum = 0;
        while (start < end) {
            toSum = numbers[start] + numbers[end];
            if (toSum == target) 
                return new int[] {start + 1, end + 1};
            else if (toSum > target)
                end--;
            else start++;
        }
        return new int[] {-1,-1};
    }
}
